import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  flexContainer: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  errorMessage: {
    textAlign: 'center',
    fontSize: 25,
    color: 'red',
  },
  successMessage: {
    textAlign: 'center',
    fontSize: 25,
    color: 'green',
  },
});
